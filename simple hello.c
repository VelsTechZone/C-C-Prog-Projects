#include<stdio.h> //header file which include standard input/output functions

int main() //every prog must contain one main func

{
    printf("Hello, World\n"); //printf - prints given input within func
    return(0);
}